# トリミングサロン スケジュール調整ツール

## 起動手順（Windows）
1. 初回のみ `setup.bat` を実行（`.venv` 作成と依存インストール）。
2. 以降は `run.bat` で起動。

## 概要
- Streamlit + SQLite で動作する、受付/管理者向けのスケジュール調整ツールです。
- 固定開始時刻は自動変更せず、仮想枠と警告表示で手動判断を支援します。
