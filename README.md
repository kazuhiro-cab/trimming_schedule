# トリミングサロン スケジュール調整ツール

## 起動手順（Windows）
1. 初回のみ `setup.bat` を実行（`.venv` 作成と依存インストール）。
2. 以降は `run.bat` で起動。

## 操作のポイント
- メインはタイムライン中心UIです。
- 予約バー（確定枠）をドラッグすると開始時刻を**手動で**変更できます。
- 衝突は自動解消せず、黄/赤警告と仮想枠の可視化のみ行います。
- 設定は歯車ボタン、予約作成は「＋予約」ボタンから開きます。
